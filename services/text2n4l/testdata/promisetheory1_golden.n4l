 - Samples from testdata/promisetheory1.dat

# (begin) ************

 :: _sequence_ , testdata/promisetheory1::

 :: promise theory, other words ::

@sen2   If such principles were any good, it shouldn't matter whether we're talking about humans in a team, birds in a flock, computers in a datacenter, or cogs in a Swiss watch.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen4   Such principles are the subject of Promise Theory.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen5   The goal of Promise Theory is reveal the behaviour of a whole from the sum of its parts, taking the viewpoint of the parts rather than the whole.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen8   I   described a more formal/mathematical account of promise theory in   Promise Theory, Principles and Applications
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen14   The word `promise' seemed a good fit for what I needed, a kind of atom for intent that, when combined, could represent a maintainable policy.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen16   Promises could also be an effective way of understanding a whole range of related issues about how parts operate as a whole, and it promised;Perhaps   the most important thing about promise theory is that it drives   people to the most terrible puns, without realizing that those puns   said something involuntarily insightful too., something not previously taken seriously, a way to unify human and machine behaviours in a single description.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen18   in business and computer science, promise theory is not a manifesto, nor is it a political statement or a philosophical agenda.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen20   It is little more than a method of analysis, for picking systems apart into their essential pieces, and then putting them back together.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen22   For some people, this is what computer programming is about, and there have been many philosophies around this, like OO, SOA, UML, etc.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen25   The purpose of this book is to ask what can an understanding in terms of promises tell us about cooperation in human-machine systems, organizations and technology, and how can we apply that understanding to the real life challenges of working together?
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen36   Consider the following instructions for cleaning a public restroom., Wash the floor with agent X, Mop and brush the bowls, Put towels in the dispenser, Refill soap, Do this every hour, on the hour ;
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen38   I promise that the floor will be clean and dry after hourly checks, I promise that the bowls will be clean and empty after hourly checks, I promise that there will be clean towels in the dispenser after hourly checks, I promise that there will be soap in the dispenser after hourly checks ;
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen41   True enough, that is the received learning since the time of Newton, but it is not really a true version of reality;For something to push or force something else normally requires an electromagnetic repulsion, which in turn requires a kind of promise from both objects to respond to the electrostatic force.
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

@sen44   Neutrinos from the sun, for example, don't promise to respond to electrostatic forces, which is why it is extremely hard for us to capture and measure them.;.;
              " (extract/quote from) part 0 of testdata/promisetheory1 with promise theory, other words
              " (in a section intentionally expressing) hourly checks
              " (in a section intentionally expressing) some people
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) taken seriously
              " (in a section intentionally expressing) insightful too
              " (in a section intentionally expressing) electromagnetic repulsion
              " (in a section intentionally expressing) promise formulation
              " (in a section intentionally expressing) became clear
              " (in a section intentionally expressing) philosophical agenda
              " (in a section intentionally expressing) intended outcomes
              " (in a section intentionally expressing) with collaborators
              " (in a section intentionally expressing) true enough
              " (in a section intentionally expressing) maintainable policy
              " (in a section intentionally expressing) this force
              " (in a section intentionally expressing) terrible puns

 :: intended outcome ::

@sen45   The first thing we notice is that some agent, [a person or robot], has to make the promise, so we know who is the active agent, and that by making the promise this agent is accepting the responsibility for it.
              " (extract/quote from) part 1 of testdata/promisetheory1 with intended outcome
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) starting point
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other words
              " (in a section intentionally expressing) some agent
              " (in a section intentionally expressing) called impositions
              " (in a section intentionally expressing) ultimate outcome
              " (in a section intentionally expressing) become clearer
              " (in a section intentionally expressing) voluntary behaviour
              " (in a section intentionally expressing) right now
              " (in a section intentionally expressing) electrostatic forces
              " (in a section intentionally expressing) active agent
              " (in a section intentionally expressing) own directives
              " (in a section intentionally expressing) why promise
              " (in a section intentionally expressing) persistent states

@sen47   ;Why is a promise better than a command?;
              " (extract/quote from) part 1 of testdata/promisetheory1 with intended outcome
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) starting point
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other words
              " (in a section intentionally expressing) some agent
              " (in a section intentionally expressing) called impositions
              " (in a section intentionally expressing) ultimate outcome
              " (in a section intentionally expressing) become clearer
              " (in a section intentionally expressing) voluntary behaviour
              " (in a section intentionally expressing) right now
              " (in a section intentionally expressing) electrostatic forces
              " (in a section intentionally expressing) active agent
              " (in a section intentionally expressing) own directives
              " (in a section intentionally expressing) why promise
              " (in a section intentionally expressing) persistent states

@sen50   In Promise Theory, these latter things are called impositions, because they impose intentions onto others without an invitation.
              " (extract/quote from) part 1 of testdata/promisetheory1 with intended outcome
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) starting point
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other words
              " (in a section intentionally expressing) some agent
              " (in a section intentionally expressing) called impositions
              " (in a section intentionally expressing) ultimate outcome
              " (in a section intentionally expressing) become clearer
              " (in a section intentionally expressing) voluntary behaviour
              " (in a section intentionally expressing) right now
              " (in a section intentionally expressing) electrostatic forces
              " (in a section intentionally expressing) active agent
              " (in a section intentionally expressing) own directives
              " (in a section intentionally expressing) why promise
              " (in a section intentionally expressing) persistent states

@sen52   Promises expose information that is relevant to an expected outcome more directly than impositions, because they always focus on the point of causation, the agent that makes the promise.
              " (extract/quote from) part 1 of testdata/promisetheory1 with intended outcome
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) starting point
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other words
              " (in a section intentionally expressing) some agent
              " (in a section intentionally expressing) called impositions
              " (in a section intentionally expressing) ultimate outcome
              " (in a section intentionally expressing) become clearer
              " (in a section intentionally expressing) voluntary behaviour
              " (in a section intentionally expressing) right now
              " (in a section intentionally expressing) electrostatic forces
              " (in a section intentionally expressing) active agent
              " (in a section intentionally expressing) own directives
              " (in a section intentionally expressing) why promise
              " (in a section intentionally expressing) persistent states

@sen60   Why is a promise better than a command?
              " (extract/quote from) part 1 of testdata/promisetheory1 with intended outcome
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) starting point
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other words
              " (in a section intentionally expressing) some agent
              " (in a section intentionally expressing) called impositions
              " (in a section intentionally expressing) ultimate outcome
              " (in a section intentionally expressing) become clearer
              " (in a section intentionally expressing) voluntary behaviour
              " (in a section intentionally expressing) right now
              " (in a section intentionally expressing) electrostatic forces
              " (in a section intentionally expressing) active agent
              " (in a section intentionally expressing) own directives
              " (in a section intentionally expressing) why promise
              " (in a section intentionally expressing) persistent states

@sen68   That, by definition, is what you don't control.;
              " (extract/quote from) part 1 of testdata/promisetheory1 with intended outcome
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) starting point
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other words
              " (in a section intentionally expressing) some agent
              " (in a section intentionally expressing) called impositions
              " (in a section intentionally expressing) ultimate outcome
              " (in a section intentionally expressing) become clearer
              " (in a section intentionally expressing) voluntary behaviour
              " (in a section intentionally expressing) right now
              " (in a section intentionally expressing) electrostatic forces
              " (in a section intentionally expressing) active agent
              " (in a section intentionally expressing) own directives
              " (in a section intentionally expressing) why promise
              " (in a section intentionally expressing) persistent states

 :: incomplete information ::

@sen91   When a change happens, we know that it happens within   an autonomous region, and it could not happen from outside, without   explicitly promising to subordinate itself to an outside influence.
              " (extract/quote from) part 2 of testdata/promisetheory1 with incomplete information
              " (in a section intentionally expressing) already happened
              " (in a section intentionally expressing) intended outcome
              " (in a section intentionally expressing) always right
              " (in a section intentionally expressing) department says
              " (in a section intentionally expressing) autonomous parts
              " (in a section intentionally expressing) with impositions
              " (in a section intentionally expressing) being promised
              " (in a section intentionally expressing) just soa
              " (in a section intentionally expressing) expectations first
              " (in a section intentionally expressing) difficult circumstances
              " (in a section intentionally expressing) outside influence
              " (in a section intentionally expressing) restroom example
              " (in a section intentionally expressing) autonomous region
              " (in a section intentionally expressing) called devops
              " (in a section intentionally expressing) into operations

@sen97   Usually a promise is about an outcome that has yet to happen, e.g.
              " (extract/quote from) part 2 of testdata/promisetheory1 with incomplete information
              " (in a section intentionally expressing) already happened
              " (in a section intentionally expressing) intended outcome
              " (in a section intentionally expressing) always right
              " (in a section intentionally expressing) department says
              " (in a section intentionally expressing) autonomous parts
              " (in a section intentionally expressing) with impositions
              " (in a section intentionally expressing) being promised
              " (in a section intentionally expressing) just soa
              " (in a section intentionally expressing) expectations first
              " (in a section intentionally expressing) difficult circumstances
              " (in a section intentionally expressing) outside influence
              " (in a section intentionally expressing) restroom example
              " (in a section intentionally expressing) autonomous region
              " (in a section intentionally expressing) called devops
              " (in a section intentionally expressing) into operations

@sen98   `I promise to get you to the church on time for your wedding', but we can also promise things that have already happened, where it is the verification that has yet to happen.;
              " (extract/quote from) part 2 of testdata/promisetheory1 with incomplete information
              " (in a section intentionally expressing) already happened
              " (in a section intentionally expressing) intended outcome
              " (in a section intentionally expressing) always right
              " (in a section intentionally expressing) department says
              " (in a section intentionally expressing) autonomous parts
              " (in a section intentionally expressing) with impositions
              " (in a section intentionally expressing) being promised
              " (in a section intentionally expressing) just soa
              " (in a section intentionally expressing) expectations first
              " (in a section intentionally expressing) difficult circumstances
              " (in a section intentionally expressing) outside influence
              " (in a section intentionally expressing) restroom example
              " (in a section intentionally expressing) autonomous region
              " (in a section intentionally expressing) called devops
              " (in a section intentionally expressing) into operations

@sen99   If your accounts department says, `I promise that I paid the bill on time', the outcome has already happened, but the promisee has not yet verified the claim.;
              " (extract/quote from) part 2 of testdata/promisetheory1 with incomplete information
              " (in a section intentionally expressing) already happened
              " (in a section intentionally expressing) intended outcome
              " (in a section intentionally expressing) always right
              " (in a section intentionally expressing) department says
              " (in a section intentionally expressing) autonomous parts
              " (in a section intentionally expressing) with impositions
              " (in a section intentionally expressing) being promised
              " (in a section intentionally expressing) just soa
              " (in a section intentionally expressing) expectations first
              " (in a section intentionally expressing) difficult circumstances
              " (in a section intentionally expressing) outside influence
              " (in a section intentionally expressing) restroom example
              " (in a section intentionally expressing) autonomous region
              " (in a section intentionally expressing) called devops
              " (in a section intentionally expressing) into operations

@sen114   In the world of information technology, design translates into `development' and maintenance translates into `operations', and understanding both together is often called DevOps.
              " (extract/quote from) part 2 of testdata/promisetheory1 with incomplete information
              " (in a section intentionally expressing) already happened
              " (in a section intentionally expressing) intended outcome
              " (in a section intentionally expressing) always right
              " (in a section intentionally expressing) department says
              " (in a section intentionally expressing) autonomous parts
              " (in a section intentionally expressing) with impositions
              " (in a section intentionally expressing) being promised
              " (in a section intentionally expressing) just soa
              " (in a section intentionally expressing) expectations first
              " (in a section intentionally expressing) difficult circumstances
              " (in a section intentionally expressing) outside influence
              " (in a section intentionally expressing) restroom example
              " (in a section intentionally expressing) autonomous region
              " (in a section intentionally expressing) called devops
              " (in a section intentionally expressing) into operations

@sen125   By telling it like you mean it, it becomes a promise.
              " (extract/quote from) part 2 of testdata/promisetheory1 with incomplete information
              " (in a section intentionally expressing) already happened
              " (in a section intentionally expressing) intended outcome
              " (in a section intentionally expressing) always right
              " (in a section intentionally expressing) department says
              " (in a section intentionally expressing) autonomous parts
              " (in a section intentionally expressing) with impositions
              " (in a section intentionally expressing) being promised
              " (in a section intentionally expressing) just soa
              " (in a section intentionally expressing) expectations first
              " (in a section intentionally expressing) difficult circumstances
              " (in a section intentionally expressing) outside influence
              " (in a section intentionally expressing) restroom example
              " (in a section intentionally expressing) autonomous region
              " (in a section intentionally expressing) called devops
              " (in a section intentionally expressing) into operations

@sen130   Obligations quickly lead to conflicts because they span a region about which we have incomplete information.
              " (extract/quote from) part 2 of testdata/promisetheory1 with incomplete information
              " (in a section intentionally expressing) already happened
              " (in a section intentionally expressing) intended outcome
              " (in a section intentionally expressing) always right
              " (in a section intentionally expressing) department says
              " (in a section intentionally expressing) autonomous parts
              " (in a section intentionally expressing) with impositions
              " (in a section intentionally expressing) being promised
              " (in a section intentionally expressing) just soa
              " (in a section intentionally expressing) expectations first
              " (in a section intentionally expressing) difficult circumstances
              " (in a section intentionally expressing) outside influence
              " (in a section intentionally expressing) restroom example
              " (in a section intentionally expressing) autonomous region
              " (in a section intentionally expressing) called devops
              " (in a section intentionally expressing) into operations

@sen149   Scientists, [with the possible exception of certain social scientists], and engineers will bristle uncomfortably at the idea of mixing something so human, like a promise or intention, with something that seems objectively measurable, like outcomes in the real world.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen153   In my book, In Search of Certainty, I describe how the very hardest natural sciences have forced science to confront the issues of observer relativity, [or what we might call `subjective issues'], in an unexpected twist of fate.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen154   As a physicist myself, it took me a while to accept that human issues really have to be represented in any study of technology, and that we can even do this without descending into fluff and whining about kittens, or moral outrage over privileged class systems, and so on.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen159   The goal of promise theory is to take change, [dynamics], and intent [semantics], and combine these into a simple engineering methodology, that recognizes the limitations of working with incomplete information.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen166   ;Is Promise Theory really a theory?;
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen167   Like any scientific method, Promise Theory is not a solution to anything specific, it is a language of information to describe and discuss cooperative behaviour.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen168   But if you operate within the framework of its assumptions and idioms, it will allow you to frame assumptions that can help you find solutions to problems of distributed information.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen170   Promise Theory is, if you like, an approach to modelling cooperative systems that allows you to ask, "how sure can we be that this will work"?
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen173   Promise Theory is also a kind of `atomic theory'.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen174   It gives a table of elements, [basic promises], from which any substance can be put together, like a chemistry of intentions [once an intention about self is made public, it becomes a promise]
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen175   SOA or Service Oriented Architecture is an example of a promise-oriented model, that is based on Web Services and APIs, because it defines autonomous services, [agents], with interfaces [APIs], each of which keeps well-documented promises.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

@sen177   The principles that Promise Theory cites exist to maintain generality, and to ensure that as few assumptions as possible are made.
              " (extract/quote from) part 3 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) distributed information
              " (in a section intentionally expressing) incomplete information
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) own minds
              " (in a section intentionally expressing) just impartial
              " (in a section intentionally expressing) about kittens
              " (in a section intentionally expressing) real world
              " (in a section intentionally expressing) which information
              " (in a section intentionally expressing) autonomous services
              " (in a section intentionally expressing) what rate
              " (in a section intentionally expressing) intent semantics
              " (in a section intentionally expressing) class systems
              " (in a section intentionally expressing) peace treaty
              " (in a section intentionally expressing) take change
              " (in a section intentionally expressing) subjective issues

 :: this book ::

@sen181   What is unusual about Promise Theory, compared to other scientific models is that it models human intentions, and it does it in a way that is completely impersonal.
              " (extract/quote from) part 4 of testdata/promisetheory1 with this book
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) its scope
              " (in a section intentionally expressing) clear meaning
              " (in a section intentionally expressing) first kind
              " (in a section intentionally expressing) any other
              " (in a section intentionally expressing) any agent
              " (in a section intentionally expressing) mathematical language
              " (in a section intentionally expressing) main concepts
              " (in a section intentionally expressing) voluntary behaviours
              " (in a section intentionally expressing) bounded rationality
              " (in a section intentionally expressing) one could
              " (in a section intentionally expressing) pre-existing promises
              " (in a section intentionally expressing) stated intention
              " (in a section intentionally expressing) mere imposition
              " (in a section intentionally expressing) concepts repeatedly

@sen182   By allowing contact with game theory models, we can also see how cooperative models ultimately have an economic explanation, [often referred to as bounded rationality]
              " (extract/quote from) part 4 of testdata/promisetheory1 with this book
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) its scope
              " (in a section intentionally expressing) clear meaning
              " (in a section intentionally expressing) first kind
              " (in a section intentionally expressing) any other
              " (in a section intentionally expressing) any agent
              " (in a section intentionally expressing) mathematical language
              " (in a section intentionally expressing) main concepts
              " (in a section intentionally expressing) voluntary behaviours
              " (in a section intentionally expressing) bounded rationality
              " (in a section intentionally expressing) one could
              " (in a section intentionally expressing) pre-existing promises
              " (in a section intentionally expressing) stated intention
              " (in a section intentionally expressing) mere imposition
              " (in a section intentionally expressing) concepts repeatedly

@sen192   ;Promise, When an intention is publicly declared to an   audience, [called its scope], it then becomes a promise.
              " (extract/quote from) part 4 of testdata/promisetheory1 with this book
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) its scope
              " (in a section intentionally expressing) clear meaning
              " (in a section intentionally expressing) first kind
              " (in a section intentionally expressing) any other
              " (in a section intentionally expressing) any agent
              " (in a section intentionally expressing) mathematical language
              " (in a section intentionally expressing) main concepts
              " (in a section intentionally expressing) voluntary behaviours
              " (in a section intentionally expressing) bounded rationality
              " (in a section intentionally expressing) one could
              " (in a section intentionally expressing) pre-existing promises
              " (in a section intentionally expressing) stated intention
              " (in a section intentionally expressing) mere imposition
              " (in a section intentionally expressing) concepts repeatedly

@sen216   Promise Theory is still an area of research, so we shouldn't imagine it has an answer to everything.
              " (extract/quote from) part 4 of testdata/promisetheory1 with this book
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) its scope
              " (in a section intentionally expressing) clear meaning
              " (in a section intentionally expressing) first kind
              " (in a section intentionally expressing) any other
              " (in a section intentionally expressing) any agent
              " (in a section intentionally expressing) mathematical language
              " (in a section intentionally expressing) main concepts
              " (in a section intentionally expressing) voluntary behaviours
              " (in a section intentionally expressing) bounded rationality
              " (in a section intentionally expressing) one could
              " (in a section intentionally expressing) pre-existing promises
              " (in a section intentionally expressing) stated intention
              " (in a section intentionally expressing) mere imposition
              " (in a section intentionally expressing) concepts repeatedly

@sen218   It starts with a minimal set of assumptions, it then goes on to describe the combined effect of all the individual promises, from the viewpoint of the different parts of the whole, forming a network of cooperation.
              " (extract/quote from) part 4 of testdata/promisetheory1 with this book
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) its scope
              " (in a section intentionally expressing) clear meaning
              " (in a section intentionally expressing) first kind
              " (in a section intentionally expressing) any other
              " (in a section intentionally expressing) any agent
              " (in a section intentionally expressing) mathematical language
              " (in a section intentionally expressing) main concepts
              " (in a section intentionally expressing) voluntary behaviours
              " (in a section intentionally expressing) bounded rationality
              " (in a section intentionally expressing) one could
              " (in a section intentionally expressing) pre-existing promises
              " (in a section intentionally expressing) stated intention
              " (in a section intentionally expressing) mere imposition
              " (in a section intentionally expressing) concepts repeatedly

@sen222   In Promise Theory, at least the version according to me, it has a specific and clear meaning.
              " (extract/quote from) part 4 of testdata/promisetheory1 with this book
              " (in a section intentionally expressing) promise theory
              " (in a section intentionally expressing) its scope
              " (in a section intentionally expressing) clear meaning
              " (in a section intentionally expressing) first kind
              " (in a section intentionally expressing) any other
              " (in a section intentionally expressing) any agent
              " (in a section intentionally expressing) mathematical language
              " (in a section intentionally expressing) main concepts
              " (in a section intentionally expressing) voluntary behaviours
              " (in a section intentionally expressing) bounded rationality
              " (in a section intentionally expressing) one could
              " (in a section intentionally expressing) pre-existing promises
              " (in a section intentionally expressing) stated intention
              " (in a section intentionally expressing) mere imposition
              " (in a section intentionally expressing) concepts repeatedly

@sen227   As you read this book, you will find that Promise Theory says a lot of things that seem obvious.
              " (extract/quote from) part 5 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) absolute certainty
              " (in a section intentionally expressing) this book
              " (in a section intentionally expressing) beneficial way
              " (in a section intentionally expressing) blunt instruments
              " (in a section intentionally expressing) irresistible forces
              " (in a section intentionally expressing) modelling perspective
              " (in a section intentionally expressing) stunning revelations
              " (in a section intentionally expressing) unfamiliar ways
              " (in a section intentionally expressing) more clearly
              " (in a section intentionally expressing) specialized meanings
              " (in a section intentionally expressing) seem obvious
              " (in a section intentionally expressing) key players
              " (in a section intentionally expressing) business-it bridge
              " (in a section intentionally expressing) good thing
              " (in a section intentionally expressing) policy documents

@sen260   Want absolute certainty?
              " (extract/quote from) part 5 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) absolute certainty
              " (in a section intentionally expressing) this book
              " (in a section intentionally expressing) beneficial way
              " (in a section intentionally expressing) blunt instruments
              " (in a section intentionally expressing) irresistible forces
              " (in a section intentionally expressing) modelling perspective
              " (in a section intentionally expressing) stunning revelations
              " (in a section intentionally expressing) unfamiliar ways
              " (in a section intentionally expressing) more clearly
              " (in a section intentionally expressing) specialized meanings
              " (in a section intentionally expressing) seem obvious
              " (in a section intentionally expressing) key players
              " (in a section intentionally expressing) business-it bridge
              " (in a section intentionally expressing) good thing
              " (in a section intentionally expressing) policy documents

@sen268   Each promise will have a probability associated with it, based on our trust or belief in its future behaviour;The simplistic two-state model of faults, where   manufacturer's like to talk of all their 9's, the expressions Mean   Time Before Failure, [MTBF], and Mean Time To Repair [MTTR], are   coined.
              " (extract/quote from) part 5 of testdata/promisetheory1 with 
              " (in a section intentionally expressing) absolute certainty
              " (in a section intentionally expressing) this book
              " (in a section intentionally expressing) beneficial way
              " (in a section intentionally expressing) blunt instruments
              " (in a section intentionally expressing) irresistible forces
              " (in a section intentionally expressing) modelling perspective
              " (in a section intentionally expressing) stunning revelations
              " (in a section intentionally expressing) unfamiliar ways
              " (in a section intentionally expressing) more clearly
              " (in a section intentionally expressing) specialized meanings
              " (in a section intentionally expressing) seem obvious
              " (in a section intentionally expressing) key players
              " (in a section intentionally expressing) business-it bridge
              " (in a section intentionally expressing) good thing
              " (in a section intentionally expressing) policy documents

 :: own behaviour, for humans ::

@sen270   In today's massive   scale environments, what used to be a small chance of failure or   MTBF gets amplified to a large one -- to counter this, we need   speedy repair, if we are going to keep our promises.;.
              " (extract/quote from) part 6 of testdata/promisetheory1 with own behaviour, for humans
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) about requirements
              " (in a section intentionally expressing) others' model
              " (in a section intentionally expressing) this crazy
              " (in a section intentionally expressing) contingency plans
              " (in a section intentionally expressing) promise viewpoint
              " (in a section intentionally expressing) keep promises
              " (in a section intentionally expressing) others' behalf
              " (in a section intentionally expressing) about them
              " (in a section intentionally expressing) command structures
              " (in a section intentionally expressing) probabilistic measures
              " (in a section intentionally expressing) single service-promise
              " (in a section intentionally expressing) voluntary cooperation

@sen273   If we try to make promises on others' behalf, they will most likely be rejected, impossible to implement, or the other agent might not even know about them.
              " (extract/quote from) part 6 of testdata/promisetheory1 with own behaviour, for humans
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) about requirements
              " (in a section intentionally expressing) others' model
              " (in a section intentionally expressing) this crazy
              " (in a section intentionally expressing) contingency plans
              " (in a section intentionally expressing) promise viewpoint
              " (in a section intentionally expressing) keep promises
              " (in a section intentionally expressing) others' behalf
              " (in a section intentionally expressing) about them
              " (in a section intentionally expressing) command structures
              " (in a section intentionally expressing) probabilistic measures
              " (in a section intentionally expressing) single service-promise
              " (in a section intentionally expressing) voluntary cooperation

@sen280   Promise theory focuses attention on the active agents for two reasons, first, because these are the ones who know most about their own ability to keep promises.
              " (extract/quote from) part 6 of testdata/promisetheory1 with own behaviour, for humans
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) about requirements
              " (in a section intentionally expressing) others' model
              " (in a section intentionally expressing) this crazy
              " (in a section intentionally expressing) contingency plans
              " (in a section intentionally expressing) promise viewpoint
              " (in a section intentionally expressing) keep promises
              " (in a section intentionally expressing) others' behalf
              " (in a section intentionally expressing) about them
              " (in a section intentionally expressing) command structures
              " (in a section intentionally expressing) probabilistic measures
              " (in a section intentionally expressing) single service-promise
              " (in a section intentionally expressing) voluntary cooperation

@sen289   you might effectively make promises on behalf of agents you don't control., On the other hand, when you work from the bottom up, you have no choice but to know where things are, because you will need to document every assumption with an explicit promise.
              " (extract/quote from) part 6 of testdata/promisetheory1 with own behaviour, for humans
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) about requirements
              " (in a section intentionally expressing) others' model
              " (in a section intentionally expressing) this crazy
              " (in a section intentionally expressing) contingency plans
              " (in a section intentionally expressing) promise viewpoint
              " (in a section intentionally expressing) keep promises
              " (in a section intentionally expressing) others' behalf
              " (in a section intentionally expressing) about them
              " (in a section intentionally expressing) command structures
              " (in a section intentionally expressing) probabilistic measures
              " (in a section intentionally expressing) single service-promise
              " (in a section intentionally expressing) voluntary cooperation

@sen307   The goal in Promise Theory cooperation is to ensure that agents make all the promises necessary so that some magical on-looker, with access to all the information, would be able to say that an entire cooperative operation could be seen as if it were a single entity making a single service-promise, [the algebra tells you if the set of promises is complete or not]
              " (extract/quote from) part 6 of testdata/promisetheory1 with own behaviour, for humans
              " (in a section intentionally expressing) atomic theory
              " (in a section intentionally expressing) don't control
              " (in a section intentionally expressing) other hand
              " (in a section intentionally expressing) about requirements
              " (in a section intentionally expressing) others' model
              " (in a section intentionally expressing) this crazy
              " (in a section intentionally expressing) contingency plans
              " (in a section intentionally expressing) promise viewpoint
              " (in a section intentionally expressing) keep promises
              " (in a section intentionally expressing) others' behalf
              " (in a section intentionally expressing) about them
              " (in a section intentionally expressing) command structures
              " (in a section intentionally expressing) probabilistic measures
              " (in a section intentionally expressing) single service-promise
              " (in a section intentionally expressing) voluntary cooperation

@sen323   The fact that the initial state of a system has unknown intentions, and distributed information, means we have to set up things like `agreements', where agents promise to behave in a certain way.
              " (extract/quote from) part 7 of testdata/promisetheory1 with 

@sen328   Promise theory is rather good at resolving conflicts, because an agent can only conflict with itself, hence all the information to resolve them is located in the same place.
              " (extract/quote from) part 7 of testdata/promisetheory1 with 

@sen330   Promise Theory seems upside down to some people.
              " (extract/quote from) part 7 of testdata/promisetheory1 with 

@sen338   Promise theory assumes that an agent can only make promises about its own behaviour, [because that is all it is in control of], and this cuts through the distributed issue, ensuring that the information and resources needed to resolve any problem are local and available to the agent, so it can autonomously repair [self-heal], a process.;
              " (extract/quote from) part 7 of testdata/promisetheory1 with 

@sen347   Don't miss anything, from the bed you get out of, to your morning exercise regimen, the food you eat, the people you work with, the city you live in, all the way up to the toothbrush you end your day with.
              " (extract/quote from) part 7 of testdata/promisetheory1 with 

# (end) ************

# Final fraction 16.25 of requested 10.00

# Selected 58 samples of 357: 2 4 5 8 14 16 18 20 22 25 36 38 41 44 45 47 50 52 60 68 91 97 98 99 114 125 130 149 153 154 159 166 167 168 170 173 174 175 177 181 182 192 216 218 222 227 260 268 270 273 280 289 307 323 328 330 338 347 
#

 :: themes and topics you might want to annotate/replace ::

 :: parts, sections ::

 part 0 of testdata/promisetheory1 with promise theory, other words
  #AMBI promise theory
  #AMBI other words
   #INTENT hourly checks
   #INTENT some people
   #INTENT other hand
   #INTENT taken seriously
   #INTENT insightful too
   #INTENT electromagnetic repulsion
   #INTENT promise formulation
   #INTENT became clear
   #INTENT philosophical agenda
   #INTENT intended outcomes
   #INTENT with collaborators
   #INTENT true enough
   #INTENT maintainable policy
   #INTENT this force
   #INTENT terrible puns

 part 1 of testdata/promisetheory1 with intended outcome
  #AMBI intended outcome
   #INTENT promise theory
   #INTENT starting point
   #INTENT don't control
   #INTENT other words
   #INTENT some agent
   #INTENT called impositions
   #INTENT ultimate outcome
   #INTENT become clearer
   #INTENT voluntary behaviour
   #INTENT right now
   #INTENT electrostatic forces
   #INTENT active agent
   #INTENT own directives
   #INTENT why promise
   #INTENT persistent states

 part 2 of testdata/promisetheory1 with incomplete information
  #AMBI incomplete information
   #INTENT already happened
   #INTENT intended outcome
   #INTENT always right
   #INTENT department says
   #INTENT autonomous parts
   #INTENT with impositions
   #INTENT being promised
   #INTENT just soa
   #INTENT expectations first
   #INTENT difficult circumstances
   #INTENT outside influence
   #INTENT restroom example
   #INTENT autonomous region
   #INTENT called devops
   #INTENT into operations

 part 3 of testdata/promisetheory1 with 
   #INTENT distributed information
   #INTENT incomplete information
   #INTENT atomic theory
   #INTENT own minds
   #INTENT just impartial
   #INTENT about kittens
   #INTENT real world
   #INTENT which information
   #INTENT autonomous services
   #INTENT what rate
   #INTENT intent semantics
   #INTENT class systems
   #INTENT peace treaty
   #INTENT take change
   #INTENT subjective issues

 part 4 of testdata/promisetheory1 with this book
  #AMBI this book
   #INTENT promise theory
   #INTENT its scope
   #INTENT clear meaning
   #INTENT first kind
   #INTENT any other
   #INTENT any agent
   #INTENT mathematical language
   #INTENT main concepts
   #INTENT voluntary behaviours
   #INTENT bounded rationality
   #INTENT one could
   #INTENT pre-existing promises
   #INTENT stated intention
   #INTENT mere imposition
   #INTENT concepts repeatedly

 part 5 of testdata/promisetheory1 with 
   #INTENT absolute certainty
   #INTENT this book
   #INTENT beneficial way
   #INTENT blunt instruments
   #INTENT irresistible forces
   #INTENT modelling perspective
   #INTENT stunning revelations
   #INTENT unfamiliar ways
   #INTENT more clearly
   #INTENT specialized meanings
   #INTENT seem obvious
   #INTENT key players
   #INTENT business-it bridge
   #INTENT good thing
   #INTENT policy documents

 part 6 of testdata/promisetheory1 with own behaviour, for humans
  #AMBI own behaviour
  #AMBI for humans
   #INTENT atomic theory
   #INTENT don't control
   #INTENT other hand
   #INTENT about requirements
   #INTENT others' model
   #INTENT this crazy
   #INTENT contingency plans
   #INTENT promise viewpoint
   #INTENT keep promises
   #INTENT others' behalf
   #INTENT about them
   #INTENT command structures
   #INTENT probabilistic measures
   #INTENT single service-promise
   #INTENT voluntary cooperation

 part 7 of testdata/promisetheory1 with 
